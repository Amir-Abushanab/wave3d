# @wave3d/vite

## 0.1.0

### Minor Changes

- Initial release. A dev-only Vite plugin that captures a wave3d poster from the browser already rendering it and writes it to disk — HMR-synced, deduped by config hash, deterministic frame, with a `registerPoster` client entry for React and `mountWave`. No headless browser required.
