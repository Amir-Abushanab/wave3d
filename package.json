{
  "name": "wave3d-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for the @wave3d packages and the Wave Studio app",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "prepare": "git config core.hooksPath .githooks",
    "hooks:install": "git config core.hooksPath .githooks",
    "dev": "pnpm --filter wave-studio dev",
    "build": "pnpm --filter @wave3d/core build:standalone && pnpm --filter wave-studio build",
    "preview": "pnpm --filter wave-studio preview",
    "typecheck": "pnpm -r typecheck",
    "lint": "oxlint .",
    "lint:fix": "oxlint . --fix",
    "format": "oxfmt .",
    "format:check": "oxfmt . --check",
    "depcruise": "dependency-cruiser apps packages",
    "knip": "knip",
    "check": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm depcruise && pnpm knip",
    "ncu": "pnpm dlx npm-check-updates"
  },
  "devDependencies": {
    "dependency-cruiser": "^18.0.0",
    "knip": "^6.23.0",
    "oxfmt": "^0.56.0",
    "oxlint": "^1.71.0",
    "typescript": "^6.0.3"
  },
  "packageManager": "pnpm@11.9.0"
}
