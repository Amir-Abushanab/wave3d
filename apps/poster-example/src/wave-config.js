// Edit this with `pnpm dev` running: the <wave-3d> hot-updates and @wave3d/vite re-captures
// public/hero.webp. Editing any other file leaves the config unchanged, so nothing is rewritten.
export const tweak = {
  quality: 0.6,
};
